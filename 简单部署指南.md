# CJPayment 简单部署指南（二进制方式）

## 🚀 超简单部署（3步搞定）

### 1. 编译打包
在开发机上执行：
```bash
./build.sh
```

### 2. 上传到服务器
```bash
scp dist/cjpayment_linux_amd64_*.tar.gz root@your-server:/opt/
```

### 3. 服务器上运行
```bash
cd /opt
tar -xzf cjpayment_linux_amd64_*.tar.gz
cd cjpayment_linux_amd64_*
./start.sh
```

**完成！** 🎉 访问 `http://your-server:8091`

---

## 📋 详细部署步骤

### 本地编译

```bash
# 1. 编译所有平台版本
./build.sh

# 编译结果：
# build/cjpayment_linux_amd64     <- Linux服务器用这个
# build/cjpayment_darwin_amd64    <- Mac Intel用这个
# build/cjpayment_darwin_arm64    <- Mac M1/M2用这个
# dist/cjpayment_linux_amd64_*.tar.gz <- 完整部署包
```

### 服务器部署

#### 方式一：最简单（推荐新手）
```bash
# 上传完整包
scp dist/cjpayment_linux_amd64_*.tar.gz root@your-server:/opt/

# 服务器上解压运行
ssh root@your-server
cd /opt
tar -xzf cjpayment_linux_amd64_*.tar.gz
cd cjpayment_linux_amd64_*
./start.sh

# 查看日志
tail -f cjpayment.log

# 停止服务
./stop.sh
```

#### 方式二：仅上传二进制文件
```bash
# 上传二进制文件
scp build/cjpayment_linux_amd64 root@your-server:/opt/cjpayment

# 上传web资源
scp -r web root@your-server:/opt/

# 服务器上运行
ssh root@your-server
cd /opt
chmod +x cjpayment
./cjpayment
```

#### 方式三：系统服务（推荐生产环境）
```bash
# 1. 创建用户
sudo useradd -r -s /bin/false -d /opt/cjpayment cjpayment

# 2. 创建目录
sudo mkdir -p /opt/cjpayment/logs
sudo chown -R cjpayment:cjpayment /opt/cjpayment

# 3. 复制文件
sudo cp cjpayment /opt/cjpayment/
sudo cp -r web /opt/cjpayment/
sudo cp cjpayment.service /etc/systemd/system/

# 4. 启动服务
sudo systemctl daemon-reload
sudo systemctl enable cjpayment
sudo systemctl start cjpayment

# 5. 查看状态
sudo systemctl status cjpayment
sudo journalctl -u cjpayment -f
```

## 🔧 配置选项

### 环境变量
```bash
export PORT=8091              # 服务端口
export GIN_MODE=release       # 生产模式
export LOG_LEVEL=info         # 日志级别
```

### 端口配置
- 默认端口：8091
- 修改方法：`PORT=9000 ./cjpayment`

### 数据存储
- 应用无需数据库，使用内存存储
- 日志文件：`cjpayment.log`
- 无需额外配置

## 📊 服务管理

### 检查服务状态
```bash
# 检查端口
netstat -tulnp | grep 8091

# 检查进程
ps aux | grep cjpayment

# 健康检查
curl http://localhost:8091/health
```

### 日志管理
```bash
# 查看实时日志
tail -f cjpayment.log

# 查看系统服务日志
sudo journalctl -u cjpayment -f

# 日志轮转（防止日志过大）
logrotate /etc/logrotate.d/cjpayment
```

### 更新版本
```bash
# 1. 停止旧服务
./stop.sh  # 或 sudo systemctl stop cjpayment

# 2. 备份旧版本
mv cjpayment cjpayment.old

# 3. 上传新版本
# （重复部署步骤）

# 4. 启动新服务
./start.sh  # 或 sudo systemctl start cjpayment
```

## 🛡️ 安全建议

### 防火墙设置
```bash
# 开放应用端口
sudo ufw allow 8091

# 如果使用Nginx代理，只开放80/443
sudo ufw allow 80
sudo ufw allow 443
```

### 反向代理（可选）
使用Nginx作为反向代理：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🔍 故障排除

### 常见问题

1. **端口被占用**
   ```bash
   # 查找占用进程
   lsof -i :8091
   # 杀掉进程
   kill -9 PID
   ```

2. **权限问题**
   ```bash
   chmod +x cjpayment
   chown cjpayment:cjpayment cjpayment
   ```

3. **找不到web资源**
   ```bash
   # 确保web目录在当前目录下
   ls -la web/
   ```

4. **服务无法启动**
   ```bash
   # 查看详细错误
   ./cjpayment --help

   # 检查系统资源
   free -h
   df -h
   ```

### 性能监控
```bash
# 系统资源
htop
iostat 1
netstat -i

# 应用监控
curl http://localhost:8091/metrics  # 如果有metrics端点
```

## 🎯 生产环境检查清单

- [ ] 服务器环境准备
- [ ] 防火墙端口开放
- [ ] 应用用户创建
- [ ] 二进制文件上传
- [ ] web资源上传
- [ ] systemd服务配置
- [ ] 服务启动测试
- [ ] 健康检查通过
- [ ] 日志轮转配置
- [ ] 监控告警设置
- [ ] 备份策略制定

## 💡 优势对比

| 方式 | 优势 | 缺点 |
|------|------|------|
| 二进制部署 | 🟢 部署简单<br/>🟢 无依赖<br/>🟢 资源占用少 | 🔴 需手动管理数据库 |
| Docker部署 | 🟢 环境一致<br/>🟢 包含数据库 | 🔴 学习成本高<br/>🔴 资源占用多 |

**推荐**：小型项目用二进制部署，大型项目用Docker部署。

---

**部署完成标志**：
- ✅ 可访问 `http://your-server:8091`
- ✅ 健康检查返回 `200 OK`
- ✅ 日志正常输出