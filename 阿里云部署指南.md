# CJPayment系统阿里云部署指南

## 📋 概述

本文档详细介绍如何在阿里云服务器上部署CJPayment企业支付系统，包括环境准备、配置优化、自动化部署、监控设置等完整流程。

## 🖥️ 服务器要求

### 最小配置
- **CPU**: 2核
- **内存**: 4GB
- **存储**: 40GB SSD
- **带宽**: 1Mbps
- **操作系统**: Ubuntu 20.04+ 或 CentOS 7+

### 推荐配置（生产环境）
- **CPU**: 4核或8核
- **内存**: 8GB或16GB
- **存储**: 100GB+ SSD
- **带宽**: 5Mbps+
- **操作系统**: Ubuntu 22.04 LTS

## 🚀 快速部署

### 1. 服务器初始化

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装基础软件
sudo apt install -y git curl wget vim unzip

# 创建应用用户
sudo useradd -m -s /bin/bash cjpayment
sudo usermod -aG docker cjpayment

# 设置用户免密sudo（可选）
echo "cjpayment ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/cjpayment
```

### 2. 安装Docker和Docker Compose

```bash
# 安装Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.21.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 启动Docker服务
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
docker --version
docker-compose --version
```

### 3. 部署应用

```bash
# 切换到应用用户
su - cjpayment

# 克隆项目
git clone <your-repository-url> cjpayment
cd cjpayment

# 配置环境变量
cp .env.aliyun .env
vim .env  # 修改密码和配置

# 执行自动部署
./deploy-aliyun.sh
```

## ⚙️ 详细配置步骤

### 1. 环境变量配置

编辑 `.env.aliyun` 文件：

```bash
# 数据库配置（必须修改）
MYSQL_ROOT_PASSWORD=your_strong_mysql_root_password
DB_PASSWORD=your_strong_db_password

# Redis配置（必须修改）
REDIS_PASSWORD=your_strong_redis_password

# 应用配置
DOMAIN=your-domain.com  # 修改为你的域名
APP_PORT=8091

# JWT密钥（必须修改为复杂字符串）
JWT_SECRET=your_very_long_and_complex_jwt_secret_here

# 阿里云配置（如需要）
ALIYUN_REGION=cn-hangzhou
ALIYUN_ACCESS_KEY_ID=your_access_key
ALIYUN_ACCESS_KEY_SECRET=your_secret_key
```

### 2. SSL证书配置

#### 选项1：使用Let's Encrypt（推荐）

```bash
# 安装Certbot
sudo apt install snapd
sudo snap install --classic certbot

# 申请证书
sudo certbot --nginx -d your-domain.com
```

#### 选项2：使用自有证书

```bash
# 将证书文件放置到ssl目录
mkdir -p ssl
cp your-domain.com.crt ssl/
cp your-domain.com.key ssl/

# 修改Nginx配置中的证书路径
vim nginx/conf.d/cjpayment.conf
```

### 3. 域名和防火墙配置

```bash
# 开放必要端口
sudo ufw allow 22      # SSH
sudo ufw allow 80      # HTTP
sudo ufw allow 443     # HTTPS
sudo ufw allow 8091    # 应用端口（可选，建议通过Nginx代理）

# 启用防火墙
sudo ufw --force enable
sudo ufw status
```

### 4. 数据库初始化

系统启动后会自动初始化数据库，如需手动操作：

```bash
# 连接数据库
docker exec -it cjpayment-mysql mysql -u cjpayment -p

# 创建示例数据（可选）
USE cjpayment;
-- 在这里添加你的初始化SQL语句
```

## 📊 监控配置

### 启用监控服务

```bash
# 启动包含监控的完整服务
./deploy-aliyun.sh --with-monitoring
```

### 访问监控面板

- **Prometheus**: http://your-domain:9090
- **Grafana**: http://your-domain:3000
  - 默认用户名: admin
  - 默认密码: 在.env文件中设置的GRAFANA_PASSWORD

### Grafana配置

1. 登录Grafana
2. 添加Prometheus数据源: http://prometheus:9090
3. 导入预设仪表板（如有提供）

## 🛠️ 运维管理

### 常用命令

```bash
# 查看服务状态
docker-compose -f docker-compose.aliyun.yml ps

# 查看日志
docker-compose -f docker-compose.aliyun.yml logs -f
docker-compose -f docker-compose.aliyun.yml logs -f cjpayment-app

# 重启服务
docker-compose -f docker-compose.aliyun.yml restart
docker-compose -f docker-compose.aliyun.yml restart cjpayment-app

# 更新应用
git pull
docker-compose -f docker-compose.aliyun.yml build cjpayment-app
docker-compose -f docker-compose.aliyun.yml up -d cjpayment-app

# 数据备份
./deploy-aliyun.sh --backup
```

### 定期维护

#### 1. 设置自动备份

```bash
# 编辑crontab
crontab -e

# 添加以下行（每天凌晨2点备份）
0 2 * * * /home/cjpayment/cjpayment/deploy-aliyun.sh --backup >> /home/cjpayment/backup.log 2>&1

# 清理旧备份（保留30天）
0 3 * * * find /home/cjpayment/cjpayment/backups -name "backup_*" -mtime +30 -exec rm -rf {} + >> /home/cjpayment/cleanup.log 2>&1
```

#### 2. 日志轮转

```bash
# 创建日志轮转配置
sudo tee /etc/logrotate.d/cjpayment << EOF
/home/cjpayment/cjpayment/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
EOF
```

#### 3. 系统监控

```bash
# 监控脚本
tee ~/check-system.sh << 'EOF'
#!/bin/bash
echo "=== $(date) ==="
echo "磁盘使用情况:"
df -h
echo "内存使用情况:"
free -h
echo "Docker服务状态:"
docker-compose -f /home/cjpayment/cjpayment/docker-compose.aliyun.yml ps
echo "应用健康检查:"
curl -s http://localhost:8091/health || echo "应用服务异常"
curl -s http://localhost/health || echo "Nginx服务异常"
echo "========================="
EOF

chmod +x ~/check-system.sh

# 添加到定时任务（每小时检查）
echo "0 * * * * /home/cjpayment/check-system.sh >> /home/cjpayment/system-check.log 2>&1" | crontab -
```

## 🔧 故障排除

### 1. 服务无法启动

```bash
# 检查Docker服务
sudo systemctl status docker

# 检查容器日志
docker-compose -f docker-compose.aliyun.yml logs

# 检查端口占用
sudo netstat -tulnp | grep :8091
sudo netstat -tulnp | grep :3306
```

### 2. 数据库连接问题

```bash
# 检查MySQL容器
docker exec -it cjpayment-mysql mysql -u root -p

# 检查数据库配置
docker exec -it cjpayment-mysql cat /etc/mysql/conf.d/my.cnf

# 重置数据库密码
docker exec -it cjpayment-mysql mysql -u root -p -e "ALTER USER 'cjpayment'@'%' IDENTIFIED BY 'new_password';"
```

### 3. 内存不足

```bash
# 添加swap空间
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### 4. SSL证书问题

```bash
# 检查证书有效性
openssl x509 -in ssl/your-domain.com.crt -text -noout

# 自动续期Let's Encrypt证书
echo "0 12 * * * /usr/bin/certbot renew --quiet && docker-compose -f /home/cjpayment/cjpayment/docker-compose.aliyun.yml restart nginx" | sudo tee -a /etc/crontab
```

## 🔒 安全配置

### 1. 防火墙配置

```bash
# 配置更严格的防火墙规则
sudo ufw reset
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow from YOUR_IP to any port 22  # 限制SSH访问
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
```

### 2. SSH安全

```bash
# 修改SSH配置
sudo vim /etc/ssh/sshd_config

# 推荐配置
Port 2222  # 更改默认端口
PermitRootLogin no
PasswordAuthentication no  # 使用密钥登录
MaxAuthTries 3

sudo systemctl restart sshd
```

### 3. 定期安全更新

```bash
# 自动安全更新
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades

# 手动更新
sudo apt update && sudo apt upgrade -y
```

## 📈 性能优化

### 1. 系统调优

```bash
# 编辑系统限制
sudo vim /etc/security/limits.conf
```

添加以下内容：
```
cjpayment soft nofile 65536
cjpayment hard nofile 65536
```

### 2. Docker优化

```bash
# Docker daemon配置
sudo tee /etc/docker/daemon.json << EOF
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
EOF

sudo systemctl restart docker
```

## 📞 技术支持

如遇到问题，可以：

1. 查看项目文档：`README.md`
2. 检查部署日志：`~/system-check.log`
3. 联系技术支持团队

## 🎉 部署检查清单

- [ ] 服务器环境准备完成
- [ ] Docker和Docker Compose安装成功
- [ ] 项目代码下载完成
- [ ] 环境变量配置正确
- [ ] SSL证书配置完成
- [ ] 防火墙规则设置正确
- [ ] 域名DNS解析正确
- [ ] 服务启动成功
- [ ] 健康检查通过
- [ ] 备份策略配置完成
- [ ] 监控服务启动（如需要）
- [ ] 访问测试通过

完成以上步骤后，您的CJPayment系统就已经成功部署到阿里云了！🚀